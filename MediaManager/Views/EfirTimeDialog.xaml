<Window x:Class="MediaManager.Views.EfirTimeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выберите время эфира"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        Background="{DynamicResource DialogBackgroundBrush}">

    <StackPanel Margin="24,16,24,20">

        <WrapPanel x:Name="buttonsPanel"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <Button Content="Отмена"
                Click="Cancel_Click"
                Width="120" Height="32"
                HorizontalAlignment="Center"
                Cursor="Hand"
                FontSize="13"
                Foreground="{DynamicResource DialogCancelFgBrush}"
                Background="{DynamicResource DialogCancelBgBrush}"
                BorderThickness="0">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="8,0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource DialogCancelHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource DialogCancelPressBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </StackPanel>
</Window>
